#
# EndpointMonitor - Ingress
#
kind: EndpointMonitor
metadata:
  name: emojivoto-web
  namespace: demo-emojivoto
spec:
  forceHttps: true
  providers: uptimeRobot
  uptimeRobotConfig:
    interval: 600
    monitorType: http
  urlFrom:
    ingressRef:
      name: emojivoto
